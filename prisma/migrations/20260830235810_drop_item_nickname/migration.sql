-- D-293 — 아이템 별칭 제거.
--
-- D-112 가 "같은 도감 아이템을 2개 보유하면 명칭이 완전히 같게 보이는 문제"
-- (E-05-03)를 유저 별칭으로 풀었는데, PM 판단으로 그 칸을 없앤다.
--
-- ⚠️ 값이 0건임을 확인하고 지웠다. 되살리려면 컬럼을 다시 만들어야 한다.
-- ⚠️ 같은 물건 2개를 구분하는 문제 자체는 남는다 — 다른 방법이 필요해지면
-- 그때 새로 설계한다.

-- AlterTable
ALTER TABLE "Item" DROP COLUMN "nickname";
