/**
 * 카테고리 key **단일 출처** (D-166).
 *
 * ## ⚠️ 왜 모아야 했나
 * 카테고리는 어드민이 런타임에 늘리게 설계돼 있고(A-01), 축·색인·조합은 전부
 * DB 에서 읽는다(`activeCategoryKeys`). 그런데 **선택 UI 8곳이 목록을 코드에
 * 박고 있었다** — 아이템 등록 폼 · 온보딩 · 프로필 설정 · 브랜드 요청 · 어드민
 * 브랜드 폼 · 브랜드 import. 어드민이 카테고리를 추가해도 **그 화면들에는
 * 나오지 않았다.** `운동` 을 추가하면서 실제로 그 8곳을 고쳐야 했다.
 *
 * `nav-items.ts` 가 `NAV_ITEMS` 를 공유하는 것과 같은 이유다 — 따로 두면 한쪽만
 * 고쳐져 화면마다 목록이 어긋난다.
 *
 * ## ⚠️ 여전히 코드다 — 근본은 DB 에서 읽는 것이다
 * 이 배열은 **어드민이 새 카테고리를 만들면 또 손으로 고쳐야 한다.** 클라이언트
 * 컴포넌트들이 목록을 쓰므로 서버에서 내려주거나 엔드포인트가 필요하고, 그건
 * 이 변경의 범위를 넘는다 → **OI-82**.
 *
 * 라벨은 여기 두지 않는다 — `messages/*.json` 의 `category.*` 다 (3개 언어).
 * 어드민 화면은 ko 단일이라(D-030) 거기서만 한국어 라벨을 함께 둔다.
 */
export const CATEGORY_KEYS = [
  "watch",
  "shoes",
  "bicycle",
  "apparel",
  "camping",
  // 배낭·등반장비 — 캠핑과 갈랐다 (D-259). 배낭을 양쪽에 두면 도감이 갈린다
  "hiking",
  "deskterior",
  // 실내 무산소(웨이트 트레이닝) 중심 (D-166)
  "workout",
] as const;

export type CategoryKey = (typeof CATEGORY_KEYS)[number];

/**
 * ⚠️ **브랜드 마스터가 있는 카테고리만** (D-044).
 *
 * 운동 종목에는 브랜드가 없다 — 브랜드 요청(S-17)·브랜드 import·어드민 브랜드
 * 폼의 선택지에서 빼야 한다. 넣으면 "목록에서 브랜드를 선택해주세요"로 막히는
 * 칸을 유저에게 보여주는 것이 된다 (D-043·D-150).
 */
export const BRANDED_CATEGORY_KEYS = CATEGORY_KEYS.filter(
  (k) => k !== "workout",
);

/**
 * 운동 카테고리 key — **아이템이 루틴뿐인 유일한 카테고리** (D-227).
 *
 * ⚠️ 코드가 카테고리를 판정하는 자리를 **한 곳으로 모은다.** "운동인가"를 여러
 * 파일에서 문자열로 비교하면 오타 하나가 조용히 통과한다. 정책 판정(도감 생성
 * 여부·마켓 노출·사진 필수)은 **플래그**가 하고(D-173·D-224·D-231), 이 상수는
 * **화면 분기**에만 쓴다 — 루틴 편집 UI 를 어디에 띄우는가 같은 것이다.
 */
export const WORKOUT_CATEGORY = "workout" satisfies CategoryKey;

