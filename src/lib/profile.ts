/**
 * 프로필 입력 제한 (myroom D-123, OI-69).
 *
 * ⚠️ **`"use server"` 파일에 두면 안 된다.** 그런 파일은 async 함수만
 * export 할 수 있어서 상수를 내보내는 순간 빌드가 깨진다.
 *
 * 방 이름은 피드·검색·마켓 카드에 **그대로 노출되는 값**이라 상한이 필요하다
 * — 아이템 명칭처럼 파생되는 값이 아니다 (D-073 과 대비).
 */
export const ROOM_NAME_MAX = 30;

/**
 * 하루기록 노트 상한 (D-148).
 *
 * ⚠️ 여기 두는 이유는 `ROOM_NAME_MAX` 와 같다 — **`"use server"` 파일은 async
 * 함수만 export 할 수 있어** 상수를 내보내면 빌드가 깨진다. 두 번째로 같은
 * 실수를 했다.
 *
 * 200자로 잡은 근거: 일기 본문이 1000자다 (D-053). 하루기록은 "한 도마리"이므로
 * 그보다 확실히 짧아야 **일기와 역할이 갈린다.**
 */
export const WEAR_NOTE_MAX = 200;

/**
 * 하루기록 댓글 상한 (D-178).
 *
 * ⚠️ 여기 두는 이유는 위 두 상수와 같다 — **`"use server"` 파일은 async 함수만
 * export 할 수 있어** 상수를 내보내면 빌드가 깨진다. 세 번째로 같은 실수를 하지 않는다.
 *
 * 300자로 잡은 근거: 하루기록 노트가 200자(`WEAR_NOTE_MAX`)다. 댓글이 본문보다
 * 길면 주인의 기록이 아니라 남의 글이 화면을 차지한다. 다만 대화라서 노트보다는
 * 조금 여유를 준다.
 */
export const COMMENT_MAX = 300;
