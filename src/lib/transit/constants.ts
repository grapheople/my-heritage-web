/**
 * 대중교통 상수 — **서버·클라이언트가 함께 쓴다** (D-321).
 *
 * ⚠️ `favorites.ts` 에 두면 안 된다. 그 파일은 `prisma` 를 들여오므로, 화면이
 * 상수 하나를 가져오는 순간 **Prisma 클라이언트가 브라우저 번들로 끌려온다.**
 */

/** 한 유저가 담을 수 있는 수 — 화면이 한눈에 들어오는 범위 */
export const MAX_FAVORITES = 8;

/**
 * 스냅샷을 **언제 낡았다고 보는가**.
 *
 * ⚠️ 버스·지하철은 1분이면 상황이 바뀐다. 다만 페이지를 열 때마다 포털을 부르면
 * 하루 한도를 금방 태우므로, 이 시간 안이면 저장된 값으로 센다.
 */
export const STALE_AFTER_SEC = 45;

/**
 * 이 시간보다 더 지난 도착은 **보여주지 않는다**.
 *
 * ⚠️ 스냅샷이 어제 것이면 카운트다운이 한참 음수가 되는데, 화면은 그것을
 * **"곧 도착"** 으로 계속 띄운다 — 오지 않을 차를 기다리게 하는 가장 나쁜
 * 거짓말이다. 조금 지난 것(1분 이내)은 실제로 막 도착한 차일 수 있어 남긴다.
 */
export const HIDE_AFTER_SEC = 60;
